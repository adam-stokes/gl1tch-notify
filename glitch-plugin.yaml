name: glitch-notify
description: "macOS menu bar notifications for gl1tch events (Swift)"
binary: glitch-notify
version: "0.2.0"
install:
  make: install
sidecar:
  description: "macOS menu bar notifications for gl1tch events"
  kind: daemon
  daemon: true
  display: systray
